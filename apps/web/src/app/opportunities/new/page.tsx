'use client';

/**
 * KAN-938 — Sub-cohort 3.3 New Opportunity (Deal) route.
 */
import { OpportunityForm } from '@/components/opportunities/opportunity-form';

export default function NewOpportunityPage() {
  return <OpportunityForm mode="create" />;
}
