import { TabPlaceholder } from "../_components/tab-placeholder";

export default function LegalTabPage() {
  return (
    <TabPlaceholder
      heading="Legal & compliance"
      body="Tax ID, jurisdiction, opt-out language, and disclosures land here in a future release."
    />
  );
}
